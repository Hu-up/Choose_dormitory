# 控制与计算机工程学院宿舍选择系统说明报告

## 一、线上入口

正式访问地址：

- 学生入口：<https://choose-dormitory.netlify.app>
- 男生填写：<https://choose-dormitory.netlify.app/male.html>
- 女生填写：<https://choose-dormitory.netlify.app/female.html>
- 管理员后台：<https://choose-dormitory.netlify.app/admin.html>

如果浏览器缓存导致页面没有刷新，可以使用带版本号的地址：

```text
https://choose-dormitory.netlify.app/male.html?v=10
https://choose-dormitory.netlify.app/female.html?v=10
https://choose-dormitory.netlify.app/admin.html?v=10
```

最近一次部署的唯一地址：

```text
https://6a7ff562c66dcdb9d50d9339--choose-dormitory.netlify.app
```

## 二、整体设计思路

本系统采用“静态网页 + Supabase 在线数据库”的方案。

前端网页部署在 Netlify 上。同学拿到链接后可以先填写个人信息；系统开放后，再点击宿舍卡片中的床位按钮进行选宿。后端数据保存在 Supabase 中，负责保存宿舍、名单、选择记录、系统开放时间和操作日志。

系统设计重点：

- 男生、女生填写页面分开，避免误选异性宿舍。
- 链接发出后，同学可以先填写个人信息；系统开放后，再点击宿舍卡片中的床位按钮进行选宿，不使用下拉框。
- 未开放时填写的姓名和学号会临时保存在同学自己的浏览器中，刷新页面后会自动恢复；确认选宿并提交成功后，这份本地草稿会自动清除。
- 学生填写姓名和学号时，前端会先根据名单提示是否存在、姓名是否匹配、性别是否正确；最终提交仍由 Supabase 服务端强校验。
- 管理员后台统一管理宿舍、选择记录、开放时间和操作日志。
- 学号格式固定为 `120242227xxx`。
- 服务端通过 `choose_dorm` 函数处理提交，避免多人同时抢同一宿舍或同一床位时超员/冲突。
- 学生确认床位后不可修改。
- 管理员可设置系统开放/关闭、开始时间、截止时间。
- 操作日志记录首次选择和系统设置变更，便于追溯。

## 三、前端文件说明

项目主要前端文件如下：

- `index.html`  
  学生入口页。展示系统入口，提供男生填写和女生填写入口。

- `male.html`  
  男生宿舍填写页。固定性别为男，只显示男生宿舍。学生可以先填写个人信息；系统开放后，再点击宿舍卡片中的 `1床`、`2床`、`3床`、`4床` 按钮进行选宿。

- `female.html`  
  女生宿舍填写页。固定性别为女，只显示女生宿舍。学生可以先填写个人信息；系统开放后，再点击宿舍卡片中的床位按钮进行选宿。

- `admin.html`  
  管理员后台。包含男生宿舍、女生宿舍、宿舍管理、选择名单、系统设置、操作日志等模块。

- `assets/dorm-data.js`  
  从 Excel 名单生成的前端共享配置，包含默认宿舍和学生名单。当前数据为 57 间宿舍、222 个床位、222 名学生。

- `assets/` 里的图片文件  
  页面背景、校标、侧边栏背景等视觉素材。

- `netlify.toml`  
  Netlify 部署配置文件，用于控制静态站点部署和缓存策略。

## 四、后端 Supabase 设计

Supabase 负责数据存储、并发控制和服务端规则校验。主要数据表如下：

### 1. `dorms`

宿舍表。

字段含义：

- `id`：宿舍唯一编号，例如 `m-6-225`
- `name`：宿舍名称，例如 `6号楼-225`
- `gender`：宿舍性别，男或女
- `capacity`：宿舍容量
- `created_at`：创建时间

作用：

- 学生页从这里读取可选宿舍。
- 提交时服务端检查宿舍是否存在、性别是否匹配、是否已满。

### 2. `records`

选择记录表。

字段含义：

- `id`：记录唯一编号
- `name`：学生姓名
- `gender`：学生性别
- `student_id`：学号
- `dorm_id`：选择的宿舍
- `bed_number`：选择的床位序号，按 1、2、3、4 排序
- `created_at`：首次提交时间
- `updated_at`：更新时间

作用：

- 保存最终确认的床位选择。
- 现在系统限制为确认后不可修改。
- 同一宿舍同一床位只能被一名学生选择。
- 正式运行后，学生提交成功会在这里生成一条记录。

### 3. `allowed_students`

允许选择宿舍的学生名单表。

字段含义：

- `student_id`：学号
- `name`：姓名
- `gender`：性别
- `created_at`：创建时间
- `updated_at`：更新时间

作用：

- 开启服务端名单强校验后，学生必须在这张表里。
- 服务端会校验学号、姓名、性别是否匹配。
- 如果网页更新了名单，但这张表没同步，可能出现“网页允许填写，但服务端拒绝”的情况。

### 4. `system_settings`

系统开放设置表。

字段含义：

- `id`：固定为 `main`
- `is_open`：系统是否开放
- `opens_at`：开始时间，可为空
- `closes_at`：截止时间，可为空
- `updated_at`：更新时间

作用：

- 管理员后台“系统设置”读取和修改这张表。
- 服务端提交函数会检查系统是否开放、是否到开始时间、是否超过截止时间。

### 5. `audit_log`

操作日志表。

字段含义：

- `id`：日志唯一编号
- `created_at`：日志时间
- `student_id`：相关学号
- `student_name`：相关姓名
- `action`：操作类型，例如首次选择、系统设置、名单修正
- `old_dorm_id`：原宿舍编号
- `old_dorm_name`：原宿舍名称
- `new_dorm_id`：新宿舍编号
- `new_dorm_name`：新宿舍名称
- `detail`：详细说明

作用：

- 记录学生首次选择。
- 记录系统开放状态、时间设置等变更。
- 发生争议时用于追溯。

## 五、Supabase SQL Editor 中各文件作用

你截图里 Supabase 的 SQL Editor 私有文件大概对应以下用途：

### 1. `Dorm Assignment with Capacit...`

早期版本的宿舍分配和容量检查脚本。

作用：

- 创建基础 `dorms`、`records` 表。
- 创建早期的 `choose_dorm` 函数。
- 实现宿舍容量检查。

现阶段不建议再优先使用这个旧脚本，除非只是查看历史。

### 2. `supabase-excel-data`

当前最重要的完整名单和宿舍数据初始化脚本。

对应本地文件：

```text
supabase-excel-data.sql
```

作用：

- 创建或更新 `dorms` 表。
- 创建或更新 `records` 表。
- 创建或更新 `allowed_students` 表。
- 从 Excel 导入 57 间宿舍、222 名学生名单。
- 更新服务端 `choose_dorm` 函数。
- 建立基础 RLS 策略和权限。

什么时候运行：

- 第一次正式配置数据库时。
- Excel 名单或宿舍信息有较大变化时。

### 3. `supabase-controls-lock-audit`

系统控制、锁定和日志功能脚本。

对应本地文件：

```text
supabase-controls-lock-audit.sql
```

作用：

- 创建 `system_settings` 表。
- 创建 `audit_log` 表。
- 修改 `choose_dorm`，增加开放时间检查。
- 增加“确认后不可修改”规则。
- 关闭公开 API 对 `records` 的直接新增、修改、删除权限。
- 让学生只能通过 `choose_dorm` 函数提交。

什么时候运行：

- 需要启用开放时间、截止时间、确认后锁定、操作日志时。

### 4. `supabase-fix-audit-log-rls`

操作日志权限修复脚本。

对应本地文件：

```text
supabase-fix-audit-log-rls.sql
```

作用：

- 修复保存系统设置时报错的问题。
- 报错信息通常是：

```text
new row violates row-level security policy for table "audit_log"
```

原因是系统设置变更时需要写入日志，但 `audit_log` 当时没有允许写入。

什么时候运行：

- 管理员后台保存系统开放状态失败时。

### 5. `supabase-bed-selection`

床位选择升级脚本。

对应本地文件：

```text
supabase-bed-selection.sql
```

作用：

- 给 `records` 表增加 `bed_number` 字段。
- 增加同一宿舍同一床位唯一约束。
- 将 `choose_dorm` 升级为提交宿舍和床位。
- 防止多人同时抢同一床位。

什么时候运行：

- 当系统需要从“只选宿舍”升级为“选宿舍 + 选床位”时。

### 5. `delete`

通常是手动清理测试数据用的 SQL。

建议只清理：

```sql
delete from public.audit_log;
delete from public.records;
```

不要删除：

- `dorms`
- `allowed_students`
- `system_settings`

否则会影响正式配置。

## 六、其他本地 SQL 文件说明

### 1. `supabase-schema.sql`

早期基础建表脚本。

作用：

- 创建 `dorms` 和 `records`。
- 创建基础 `choose_dorm`。

现在已经被更完整的 `supabase-excel-data.sql` 和 `supabase-controls-lock-audit.sql` 替代。

### 2. `supabase-safe-rerun.sql`

安全重复运行脚本。

作用：

- 在旧数据可能存在问题时，备份并修复 `records`。
- 删除格式不正确的学号。
- 去除重复学号。

适合数据库早期调试时使用。

### 3. `supabase-update-student-sunchen.sql`

单独修正孙晨名单的脚本。

作用：

- 将 `120242227362 / 易嘉耀` 修正为 `120242227157 / 孙晨`。
- 添加一条名单修正日志。

如果已经重新运行过完整的 `supabase-excel-data.sql`，这个脚本可以不再单独运行。

### 4. `supabase-cleanup-concurrency-test.sql`

清理并发测试数据脚本。

作用：

- 删除并发测试中成功抢到 `6号楼-225` 的 4 条测试记录。
- 删除对应操作日志。

它只清理指定测试学号，不影响宿舍和名单。

## 七、运行流程

### 1. 管理员准备阶段

1. 准备 Excel 名单和宿舍数据。
2. 运行本地脚本提取 Excel：

```text
tools/extract_dorm_excel.py
```

生成：

- `assets/dorm-data.js`
- `supabase-excel-data.sql`

3. 在 Supabase SQL Editor 运行：

```text
supabase-excel-data.sql
```

4. 再运行：

```text
supabase-controls-lock-audit.sql
```

5. 如果保存系统设置失败，运行：

```text
supabase-fix-audit-log-rls.sql
```

6. 部署网页到 Netlify。

### 2. 管理员开放系统

1. 打开管理员后台：

```text
https://choose-dormitory.netlify.app/admin.html
```

2. 进入“系统设置”。
3. 设置系统状态为“开放”。
4. 按需要设置开始时间和截止时间。
5. 点击“保存设置”。

### 3. 学生选择宿舍

1. 学生进入入口页：

```text
https://choose-dormitory.netlify.app
```

2. 男生进入男生填写页，女生进入女生填写页。
3. 系统未开放时，学生可以先填写姓名、学号等个人信息；页面会先提示名单或信息错误，但不能提交宿舍和床位。
4. 系统开放后，学生点击仍有空床位的宿舍卡片。
5. 在宿舍卡片中选择 `1床`、`2床`、`3床`、`4床` 之一。
6. 服务端执行 `choose_dorm`。
7. 如果校验通过，写入 `records`，同时保存宿舍号和床位号，并写入 `audit_log`。
8. 确认后不可修改。

### 4. 服务端提交校验顺序

学生提交时，数据库按以下顺序检查：

1. 系统是否开放。未开放时只允许学生在前端先填写个人信息，服务端不允许提交宿舍和床位。
2. 是否未到开始时间。
3. 是否超过截止时间。
4. 学号格式是否为 `120242227xxx`。
5. 学号是否在 `allowed_students`。
6. 姓名是否和名单匹配。
7. 性别是否和名单匹配。
8. 该学号是否已经确认过床位。
9. 宿舍是否存在。
10. 宿舍性别是否匹配。
11. 床位序号是否有效。
12. 该床位是否已经被占用。
13. 宿舍是否已满。
14. 通过后写入 `records` 和 `audit_log`。

## 八、测试流程

### 1. 基础测试

1. 管理员后台将系统设置为开放。
2. 使用名单内学生测试提交。
3. 再用同一学号换宿舍，应该提示不可修改。
4. 使用不存在的学号测试，应该被拒绝。
5. 使用姓名和学号不匹配的数据测试，应该被拒绝。
6. 设置截止时间为过去时间，学生提交应该被拒绝。

### 2. 并发测试

本地脚本：

```text
tools/concurrency_test_6_225.py
```

测试结果：

```text
20 个学生同时抢 6号楼-225
宿舍容量 4
成功 4 人
满员 16 人
没有超员
```

这说明服务端并发控制有效。

### 3. 测试数据清理

正式发布前，如果要清空全部测试选择记录和日志，可以运行：

```sql
delete from public.audit_log;
delete from public.records;

select
  'test data cleaned' as status,
  (select count(*) from public.records) as remaining_records,
  (select count(*) from public.audit_log) as remaining_logs;
```

这个脚本只清空测试选择记录和日志，不会删除宿舍和名单。

## 九、正式发布前检查清单

正式发给同学前，建议确认：

- `dorms` 有 57 间宿舍。
- 总床位数为 222。
- `allowed_students` 已导入完整名单。
- `records` 已清空测试记录。
- `audit_log` 已按需要清空测试日志。
- `system_settings` 设置为开放。
- 开始时间和截止时间正确。
- 男生入口、女生入口都能打开。
- 管理员后台能查看选择名单和操作日志。

## 十、维护建议

### 名单修改

如果只是单个学生信息修改，可以写一个小 SQL 更新 `allowed_students`。

如果名单或宿舍变化较多，建议重新从 Excel 生成：

```text
assets/dorm-data.js
supabase-excel-data.sql
```

然后重新部署网页，并在 Supabase 运行新的 SQL。

### 测试数据处理

测试期间产生的数据在：

- `records`
- `audit_log`

正式发布前清空这两张表即可。

### 不建议随意改动

不要随意删除：

- `dorms`
- `allowed_students`
- `system_settings`

这些是系统运行基础配置。
